<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.iqkv</groupId>
        <artifactId>boot-parent-pom</artifactId>
        <version>0.25.0-SNAPSHOT</version>
    </parent>

    <artifactId>iqscaffold-user-service</artifactId>
    <packaging>jar</packaging>

    <name>IQ Scaffold User Service</name>
    <description>Centralized authentication and user management service</description>
    <properties>
        <spring-boot.run.jvmArguments>-Duser.timezone=UTC</spring-boot.run.jvmArguments>
        <checkstyle.skip>false</checkstyle.skip>
        <maven.gitcommitid.skip>false</maven.gitcommitid.skip>
        <jacoco.skip>true</jacoco.skip>
    </properties>
    <dependencies>
        <!-- Spring Boot starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-authorization-server</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.session</groupId>
            <artifactId>spring-session-data-redis</artifactId>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
        </dependency>



        <!-- OpenAPI documentation -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>

        <!-- Observability -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-otel</artifactId>
        </dependency>

        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-exporter-otlp</artifactId>
        </dependency>

        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aspectj</artifactId>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- H2 Database for testing -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Embedded Redis for testing -->
        <dependency>
            <groupId>it.ozimov</groupId>
            <artifactId>embedded-redis</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>slf4j-simple</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Spring AMQP Test -->
        <dependency>
            <groupId>org.springframework.amqp</groupId>
            <artifactId>spring-rabbit-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Hibernate Second Level Cache with Ehcache 3 -->
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-jcache</artifactId>
        </dependency>

        <dependency>
            <groupId>org.ehcache</groupId>
            <artifactId>ehcache</artifactId>
            <classifier>jakarta</classifier>
        </dependency>

        <!-- MinIO/S3 Client -->
        <dependency>
            <groupId>io.minio</groupId>
            <artifactId>minio</artifactId>
        </dependency>

        <!-- Apache Tika for file type detection -->
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-core</artifactId>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <!-- Application entry point -->
                        <exclude>**/*Application.class</exclude>

                        <!-- Configuration classes -->
                        <exclude>**/*Config.class</exclude>
                        <exclude>**/*Config$*.class</exclude>
                        <exclude>**/*Configuration.class</exclude>
                        <exclude>**/*Configuration$*.class</exclude>
                        <exclude>**/*Properties.class</exclude>
                        <exclude>**/*Properties$*.class</exclude>

                        <!-- DTOs and Records -->
                        <exclude>**/dto/*.class</exclude>
                        <exclude>**/*Dto.class</exclude>
                        <exclude>**/*Dto$*.class</exclude>
                        <exclude>**/*Request.class</exclude>
                        <exclude>**/*Response.class</exclude>
                        <exclude>**/*Detail.class</exclude>
                        <exclude>**/*Data.class</exclude>

                        <!-- Constants -->
                        <exclude>**/*Constants.class</exclude>
                        <exclude>**/*Constants$*.class</exclude>
                        <exclude>**/*ClaimNames.class</exclude>

                        <!-- Exceptions -->
                        <exclude>**/*Exception.class</exclude>

                        <!-- REST Resources (Controllers) -->
                        <exclude>**/*RestResource.class</exclude>

                        <!-- Filters -->
                        <exclude>**/*Filter.class</exclude>

                        <!-- Health Indicators -->
                        <exclude>**/*HealthIndicator.class</exclude>

                        <!-- Metrics -->
                        <exclude>**/*Metrics.class</exclude>

                        <!-- Serializers/Deserializers -->
                        <exclude>**/*Serializer.class</exclude>
                        <exclude>**/*Deserializer.class</exclude>

                        <!-- Liquibase initializers -->
                        <exclude>**/*Initializer.class</exclude>
                        <exclude>**/*Runner.class</exclude>
                        <exclude>**/*DependencyConfigurer.class</exclude>

                        <!-- Loggers -->
                        <exclude>**/*Logger.class</exclude>

                        <!-- Event Listeners -->
                        <exclude>**/*Listener.class</exclude>
                        <exclude>**/*EventListener.class</exclude>

                        <!-- Event classes (messaging) -->
                        <exclude>**/infrastructure/messaging/*Event.class</exclude>
                        <exclude>**/MerchantCapabilitiesUpdatedEvent.class</exclude>
                        <exclude>**/MerchantOnboardedEvent.class</exclude>

                        <!-- Utility classes -->
                        <exclude>**/util/*Generator.class</exclude>
                        <exclude>**/tenancy/TenantIdGenerator.class</exclude>

                        <!-- Provisioning orchestration service -->
                        <exclude>**/tenancy/SelfServiceProvisioningService.class</exclude>

                        <!-- Complex controllers with many dependencies -->
                        <exclude>**/usermanagement/FeatureAccessController.class</exclude>
                        <exclude>**/usermanagement/FeatureAccessController$*.class</exclude>

                        <!-- File storage service requiring MinIO client mocking -->
                        <exclude>**/shared/MinioFileStorageService.class</exclude>

                        <!-- Avatar management requiring complex UserContext mocking -->
                        <exclude>**/usermanagement/AvatarManagementService.class</exclude>
                        <exclude>**/usermanagement/AvatarManagementService$*.class</exclude>

                        <!-- Authority initialization requiring complex configuration mocking -->
                        <exclude>**/shared/AuthorityInitializationService.class</exclude>

                        <!-- Inner record classes from services (DTOs) -->
                        <exclude>**/shared/MicroserviceAccessService$*.class</exclude>
                        <exclude>**/shared/ConfigurableFeatureService$*.class</exclude>

                        <!-- Complex configuration-driven service with partial coverage -->
                        <exclude>**/shared/ConfigurableFeatureService.class</exclude>

                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>coverage-check</id>
                        <phase>test</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>INSTRUCTION</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.5</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                                <rule>
                                    <element>CLASS</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.4</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.4</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
