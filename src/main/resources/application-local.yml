# Local Development Environment Configuration
# This configuration is optimized for development with debug logging and relaxed security

spring:
  jpa:
    show-sql: true

iqscaffold:
  database:
    url: ${IQSCAFFOLD_DATABASE_URL:jdbc:postgresql://localhost:5432/iqscaffold_user_local}

  cache:
    redis:
      key-prefix: "iqscaffold:auth:local"
      enable-statistics: true

  auth:
    jwt:
      secret-key: ${JWT_SECRET_KEY:local-development-secret-key-not-for-production-use}
      access-token-expiry: PT30M
      refresh-token-expiry: P30D
      issuer: iqscaffold-user-service-local
      audience: iqscaffold-services-local
    security:
      password:
        require-special-chars: false
        min-length: 6
      rate-limiting:
        login-attempts: 10
        lockout-duration: PT5M
      session:
        timeout: PT8H
        concurrent-sessions: 5

  email:
    smtp:
      port: ${SMTP_PORT:1025}
      auth: false
      starttls: false
    sender:
      from-email: ${EMAIL_FROM_EMAIL:noreply@iqscaffold.local}
      from-name: ${EMAIL_FROM_NAME:IQ Scaffold Platform (Local)}
      base-url: ${APP_BASE_URL:http://localhost:3000}
      auth-base-url: ${AUTH_BASE_URL:http://localhost:3001}
    verification:
      rate-limit: 10
    templates:
      verification-subject: "[LOCAL] Verify your IQ Scaffold account"
      password-reset-subject: "[LOCAL] Reset your IQ Scaffold password"
      password-reset-confirmed-subject: "[LOCAL] Password Reset Successful - IQ Scaffold"
      registration-confirmed-subject: "[LOCAL] Welcome to IQ Scaffold - Email Verified!"

  object-storage:
    bucket-name: ${IQSCAFFOLD_OBJECTSTORAGE_BUCKET_NAME:user-avatars-local}

  bootstrap:
    default-tenant:
      enabled: true
      tenant-id: default
      name: Local Development Tenant
      description: Local development tenant for testing
      max-users: 50
      storage-quota-gb: 10
      api-rate-limit: 10000

  # Enable manual tenant provisioning endpoints for development
  tenancy:
    manual-provisioning:
      enabled: true

  observability:
    metrics:
      prefix: iqscaffold_user_local
      custom-tags:
        environment: local
    logging:
      level: DEBUG
      enable-sql-logging: true
      enable-performance-logging: true

logging:
  level:
    com.iqscaffold: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.zaxxer.hikari: DEBUG
    org.springframework.amqp: DEBUG
    org.springframework.rabbit: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
